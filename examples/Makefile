ROOT=..
MAINPARTS= $(ROOT)/doctype.t $(ROOT)/body.t $(ROOT)/footer.t \
	$(ROOT)/setup.t $(ROOT)/menu.t $(ROOT)/css.t _example-templ.html
ACTION=$(HOME)/bin/cpp -WWW -Uunix -H -I$(ROOT) -C -V -LL $< $@

# generated by mkexam.pl, assignes the EXAMPLES variable
-include Makefile.exhtml

all: $(EXAMPLES) index.html

index.html: index.t $(MAINPARTS) /home/dast/src/daily-libssh2/libssh2/example/Makefile
	$(ACTION)

-include Makefile.examples

clean:
	rm -f $(EXAMPLES) *.et

scan:
	perl mkexam.pl
